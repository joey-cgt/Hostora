# PROJECT_CONTEXT

最后核对：2026-09-14  
项目路径：`E:\01_Workspace\Project\主持人IP视觉系统_V1.0`

本文是当前工作区的交接快照。状态以磁盘内容和 Git 工作区为准；“已完成”不等同于已经提交到 Git。

流程更新（2026-09-16）：新生成的封面和个人介绍页均依据模板结构图、风格参考图、客户原始照片及文案，在同一次图像生成请求中直接产出完整页面。具体执行以 `AGENTS.md`、`00_系统配置/MASTER_RULE.md` 和 `00_系统配置/WORKFLOW.md` 为准；本文记录的旧候选素材和旧制作方式只作历史参考。

## 1. 项目目标

「主持人IP视觉系统」是一套以文件库和规则驱动的主持人个人品牌视觉生产系统。它将以下四类输入组合为标准化视觉作品：

- 页面模板：定义版式结构、信息层级、人物与文字区域以及留白。
- 视觉风格：定义色彩、材质、光影、字体和装饰语言。
- 客户资料：提供真实人物照片、身份资料和可用文案。
- Workflow：校验输入、选择生成流程、保存候选版本、接收反馈并在确认后归档。

当前已定义两类页面：主持人封面（Workflow_A）和主持人个人介绍页（Workflow_B）。系统的硬性边界是：不得改变模板核心结构，不得编造客户信息，不得用 AI 生成的人物替代客户真实照片。

规则优先级为：`MASTER_RULE.md` > 页面 `template.md` > 风格 `style.md` > 客户 `profile.md` / `copy.md` > 模型自身创作判断。

## 2. 当前目录结构

```text
.
├── AGENTS.md                         # 指令解析、Workflow 路由、生成模式识别
├── PROJECT_CONTEXT.md                 # 本交接文档
├── 00_系统配置
│   ├── MASTER_RULE.md                 # 全局设计、真实性、输出及版本规则
│   └── WORKFLOW.md                    # Workflow_A / Workflow_B 与迭代、归档流程
├── 01_页面模板库
│   ├── A_封面
│   │   ├── A01_顶部标题人物居中型/     # template.md + structure.png
│   │   ├── A02_左横标题+横副标题_右人物型/ # template.md + structure.png
│   │   └── A03_左竖标题+横副标题_右人物型/ # template.md + structure.png
│   └── B_个人介绍页
│       ├── B01_顶部介绍+双数据卡片+双人物型/ # preview.png + structure.png + template.md
│       ├── B02_/                     # preview.png + structure.png + template.md
│       └── B03_/                     # 仅 preview.png
├── 02_视觉风格库
│   ├── S01_清透高级蓝白商业风/         # style.md + reference.jpg
│   ├── S02_红白现代商业品牌风/         # style.md + reference.jpg
│   ├── S03_深色青绿高级品牌风/         # style.md + reference.jpg
│   ├── S04_深蓝冰川科技商务风/         # style.md + reference.jpg
│   ├── S05_黑金奢华庆典风/             # style.md + reference.jpg
│   ├── S06_奶油米白婚礼极简风/         # style.md + reference.jpg
│   ├── S07_香槟金轻奢商务风/           # style.md + reference.jpg
│   └── S08_香槟雅韵东方婚礼美学风/      # style.md + reference.png
├── 03_客户资料库
│   └── 客户001_陈肖恩/                 # profile.md、copy.md、portraits/照片.png
├── 04_输出案例库
│   └── 陈果通/Workflow_A_主持人封面/A02_S02/
│       ├── final.png
│       ├── config.md
│       └── review.md
└── 05_迭代过程库
    ├── 张三/Workflow_A_主持人封面/A02_S02/V001/
    │   ├── image.png
    │   └── prompt.md
    └── 陈肖恩/Workflow_A_主持人封面/
        ├── A03_S02/V001/             # image.png + prompt.md
        └── A03_S05/V001/             # image.png + prompt.md
```

仓库中没有 `README.md`、可执行应用源码、包管理清单、CI 配置或自动化测试。`04_Prompt模板库` 已在历史提交 `4b70978` 中删除；当前提示词记录随每个迭代版本保存在 `prompt.md`。

## 3. 已完成功能

### 规则与流程

- 已建立全局规则：结构优先、风格独立、人物真实性、文案真实性、缺失内容留白、候选版本不可覆盖等。
- 已定义 Workflow_A：读取 A 模板、S 风格、客户资料并生成主持人封面。
- 已定义 Workflow_B：读取 B 模板、S 风格、客户资料并生成主持人个人介绍页。
- 已定义迭代与归档约定：候选版进入 `05_迭代过程库`，用户确认后写入 `04_输出案例库`，归档应包含 `final.png`、`config.md`、`review.md`。

### 模板与风格资产

- 已完成 A01、A02、A03 三个封面模板的 `template.md` 和 `structure.png`。
- 已建立 S01–S08 八套视觉风格，每套均有 `style.md` 和视觉参考图。
- 已完成 B01「顶部介绍 + 双数据卡片 + 双人物型」的结构化定义：`structure.png` 与 `template.md` 已根据 `preview.png` 建立。B01 将手机外框、状态栏和图外批注排除在成品版式之外；数据卡片和文案只能使用客户真实资料，缺失时留白。

### 客户与视觉产出

- 当前实际客户资料为“客户001_陈肖恩”：姓名陈肖恩、英文名 Shawn、厦门婚礼主持人；资料含一张真实人物照片。资料强调不改变五官、脸型、发型和身材比例，允许光影、背景和色彩优化。
- 已生成陈肖恩的两张封面候选图，均为 A03：S02 红白现代商业品牌风 V001 与 S05 黑金奢华庆典风 V001。它们均未归档为最终版，未记录用户反馈。
- 已归档一张陈果通（Joey）的 A02 + S02 封面最终版。`config.md` 标记为 2026-09-08 定稿，`review.md` 说明该图遵守了 A02 左文右人结构和 S02 红白现代商业风。

## 4. 正在开发功能

当前没有正在运行的生成任务或应用开发分支。工作区中正在形成的内容是：

- B 系列个人介绍页模板库的建设。B01、B02 已补齐结构图与模板说明，但仍是未跟踪文件；B03 只保留预览图，尚未结构化。
- 陈肖恩的 A03 封面候选方案仍处于等待反馈状态，可在用户提出“修改一下 / 优化一下 / 重新生成”等要求后，按 Workflow_A 创建对应 `feedback.md` 并生成 V002。

## 5. 未完成任务

1. 为 B03 从其 `preview.png` 提炼页面结构，补充 `structure.png` 与 `template.md`。
2. 实际执行至少一个 Workflow_B 个人介绍页生成任务；当前只有 B01 模板定义，没有 B 页候选版或正式案例。
3. 扩充并规范客户资料。B01 需要真实的主持年限、主持场次、个人定位、主持特点文案以及最好两张可用照片；当前陈肖恩的 `copy.md` 没有这些精确数据，生成时必须留白，不能推测。
4. 为陈肖恩候选封面收集用户反馈；如有迭代，补建 `feedback.md` 并新增 V002，不能覆盖 V001。
5. 统一模板、Workflow、候选提示词和案例配置中的画布比例表述（详见“已知 Bug”）。
6. 清理并规范 Git 追踪策略：恢复或迁移已删除的历史客户资料、处理被 `.gitignore` 忽略的真实客户目录，并将确认应保留的 B 系列和迭代文件加入版本控制。
7. 新增项目 README。`AGENTS.md` 对新增 Workflow 要求更新 README，但仓库目前没有该文件。
8. 如要把项目产品化，需要补充命令解析/参数校验的可执行实现、生成适配层、文件完整性校验和自动化测试；当前这些能力只存在于 Markdown 规则中。

## 6. 当前代码状态

这是一个“文档 + 图像资产”仓库，严格说没有业务代码。行为由 `AGENTS.md`、`MASTER_RULE.md`、`WORKFLOW.md` 和各模板/风格/客户 Markdown 文件描述，生成图像通过外部图像生成能力完成。

Git HEAD 当前为 `2e11c66 提交gitignore`；最近历史还包括 `4b70978 删除04_Prompt模板库` 和 `40567d7 add first commit`。本地工作区不是干净状态：

| 状态 | 内容 | 影响 |
| --- | --- | --- |
| 已删除（相对 HEAD） | `03_客户资料库/客户001_陈小明/` 下的 `profile.md`、`copy.md`、`portraits/照片.png` | 历史客户样例不存在于当前磁盘 |
| 未跟踪 | `01_页面模板库/B_个人介绍页/B01_顶部介绍+双数据卡片+双人物型/`、`B02_/`、`B03_/` | B 系列资产尚未提交 |
| 未跟踪 | `05_迭代过程库/陈肖恩/` 下两个 A03 V001 候选版本 | 候选记录尚未提交 |
| 被忽略 | `03_客户资料库/客户001_陈肖恩/` | 实际客户资料无法按当前 `.gitignore` 规则正常加入 Git |
| 已跟踪 | A 模板、S 风格、陈果通归档案例、张三候选记录和系统规则 | 可从当前 HEAD 恢复 |

在本次规范迁移前，已有候选图和最终图均为 1080 × 2160 px。自 2026-09-16 起，新生成的标准画布为 1088 × 2176 px（1:2），以符合 `gpt-image-2` 的 16 像素倍数约束；历史图及其记录保持原始尺寸，不追溯修改。A 系列、B01 与 B02 的 `structure.png` 都是说明图（887 × 1774 px），不应作为最终作品尺寸标准。B01 preview 为 1024 × 1536 px；B02 为 1198 × 1313 px，B03 为 1086 × 1449 px，均仅为参考图。

## 7. 已知 Bug 与一致性问题

### P1：画布比例存在系统性矛盾

已决议：新生成规范统一为 `1088 × 2176 px、1:2`。该尺寸的宽高均为 16 的倍数，可由 `gpt-image-2` CLI/API 直接请求。活动模板的旧尺寸与 A01 中遗留的“9:16”标注已完成迁移；迭代记录、归档案例和其中的提示词仍是历史事实，保持原样。

### P1：客户资料 Git 忽略规则指向失效目录

`.gitignore` 忽略整个 `03_客户资料库/*`，只反忽略 `客户001_陈小明/`；当前磁盘中的客户是 `客户001_陈肖恩/`，因此该真实资料被 Git 忽略。与此同时，HEAD 中的陈小明资料已被本地删除。此状态会导致客户资产和生成记录无法完整协作或恢复。

### P1：归档案例引用了不存在的源资料路径

陈果通案例的 `config.md` 记录源资料目录为 `客户001_张三`，当前 `03_客户资料库` 下不存在该目录。候选版本目录名同样是“张三”，但文件正文指出真实姓名为陈果通。这使归档不可完全复现；应恢复受控的源资料、迁移为正确目录，或在配置中明确资料已脱敏/移除。

### P2：Workflow 规定与已有迭代文件不完全一致

`WORKFLOW.md` 要求每个版本包含 `image.png`、`prompt.md`，并在收到修改要求后创建当前版本 `feedback.md`。现有三份 V001 都有图和提示词，未发现 feedback 文件；这在没有用户修改反馈时是符合“首次生成无需 feedback.md”的，但尚没有机制自动校验该约定。也没有 V002 示例可验证版本递增与反馈闭环。

### P2：B01 可生成信息不足

B01 结构需要主持年限、主持场次、定位短句与特点文案。当前陈肖恩资料含定位和品牌关键词，但 `copy.md` 中多处为“填写/填写个人介绍”等占位文本，且未提供真实年限、场次或 B01 所需的完整特点文案。按规则应留白，不能以 preview 的示例数值补齐。

### P2：项目说明与实际资产不一致

`AGENTS.md` 将“Prompt模板库”列为系统组成部分，但其目录已被删除；当前可用提示词只位于迭代版本 `prompt.md`。需要决定恢复集中式 Prompt 模板库，或更新说明以匹配现状。

### P3：Git 环境告警

运行 Git 时出现 `C:\\Users\\Tong/.config/git/ignore` 无法访问的权限警告。它不影响本仓库当前的状态读取，但会污染 Git 命令输出；应由环境所有者修复全局 excludesfile 的路径/权限，或调整 Git 全局配置。

## 8. 下一步开发计划

建议按以下顺序推进：

1. 已完成画布规范决策：新生成统一使用 1088×2176、1:2；历史 1080×2160 成品及其记录保持原样。
2. 修复 `.gitignore` 与客户目录命名，决定客户资料是否应受版本控制；对陈果通/张三的历史资料采用恢复、迁移或显式脱敏的方式，使已归档案例可追溯。
3. 将 B01、B02、B03 模板资产纳入版本控制。B03 完成结构图和模板文档前，不应接受对应 Workflow_B 生产任务。
4. 收集陈肖恩的真实 B01 所需数据和第二张可用照片，再执行 `B01 + Sxx + 客户001_陈肖恩` 的首版 Workflow_B，并写入 `05_迭代过程库` 的 V001。
5. 对陈肖恩现有 A03 S02 与 A03 S05 候选版获取用户意见；如继续，创建 V001/feedback.md 后生成 V002；如定稿，按约定归档至 `04_输出案例库`。
6. 增加 `README.md`，说明仓库安装/使用方式、命令格式、目录约定、比例规范及 Git 追踪策略；随后再考虑把规则实现为可执行校验工具和测试。

## 9. 重要技术决策

- 系统采用“结构、风格、客户资料分离”的组合式架构。模板只能控制布局，风格只能控制视觉语言，客户资料是人物和事实的唯一来源。
- 人物真实身份是不可突破的约束：以客户原始照片作为整页一次生成的人物输入，可在该次生成中优化背景融合、光影和色彩；不允许生成替代人物、改脸、改年龄、改服装职业属性或添加不存在的配饰。
- 缺失客户资料必须留白。尤其是经历、荣誉、案例、年限和场次，不允许根据模板示例或模型常识填补。
- 第一次生成不是最终交付。所有候选图先进入 `05_迭代过程库`，每次新生成递增版本号，旧版本保留；只有用户明确表示“可以 / 定稿 / 最终版”才进入 `04_输出案例库`。
- 最终归档应同时保存视觉结果、生成配置和设计复盘，以保证日后能说明模板、风格、人物来源和文字来源。
- B01 的关键版式决策是：顶部居中介绍、两张并列数据卡、左侧辅助照片与特点文案、右侧定位与人物主体、底部约 6% 留白。手机外框和预览批注是参考图表现，不属于成品。
- 图像生成提示词将模板结构图作为强制布局输入、风格图作为视觉参考、客户照片作为唯一人物来源；不得复制参考图中的人物、文字、品牌或未定义模块。
- 新生成画布统一为 1088 × 2176 px、1:2；该尺寸满足 `gpt-image-2` CLI/API 的 16 像素倍数要求。历史的 1080 × 2160 px 产物仅作为旧规范记录保留。

## 10. 交接操作清单

下一位维护者在任何生成任务前，应依次执行：

1. 阅读 `00_系统配置/MASTER_RULE.md`、`AGENTS.md` 和对应 Workflow。
2. 校验模板编号、风格编号、客户目录、`profile.md`、`copy.md` 与 portrait 素材是否齐全。
3. 读取目标模板的 `template.md` / `structure.png` 和目标风格的 `style.md` / reference 图。
4. 确认客户文案中每一项均为真实、可用信息；不完整则先留白或向资料提供方补齐。
5. 从上述原始输入一次生成整页结果图；首次产出写入 `05_迭代过程库/<客户>/<Workflow>/<模板>_<风格>/V001/`，至少保存 `image.png` 与 `prompt.md`。
6. 用户提出修改时，在当前版本写 `feedback.md`，再创建下一个递增版本；不要覆盖旧图。
7. 只有用户明确确认时，才复制最佳版本到 `04_输出案例库` 并补齐 `final.png`、`config.md`、`review.md`。

在开始新生产任务之前，优先处理本文件第 7 节的三个 P1 问题；它们会直接影响画布正确性、资料可追溯性与 Git 可恢复性。
